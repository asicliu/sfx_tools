{
  "name": "pdf-watermark-js",
  "version": "2.0.0",
  "description": "Client-side browser app for adding text watermarks to PDF files.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "start": "npm run dev",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "smoke": "node scripts/smoke-test.mjs",
    "check": "npm run build && npm run smoke"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "pako": "^2.1.0",
    "pdf-lib": "1.17.1",
    "spark-md5": "^3.0.2"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  },
  "engines": {
    "node": ">=18"
  }
}
