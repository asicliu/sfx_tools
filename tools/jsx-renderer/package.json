{
  "name": "jsx-renderer",
  "version": "1.0.0",
  "description": "Client-side browser app for rendering React JSX files and exporting them to HTML.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "start": "npm run dev",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "smoke": "node scripts/smoke-test.mjs",
    "check": "npm run build && npm run smoke"
  },
  "dependencies": {
    "@babel/standalone": "^7.28.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^5.4.21"
  },
  "engines": {
    "node": ">=18"
  }
}
