{
  "name": "markdown-to-pdf",
  "version": "1.0.0",
  "description": "Client-side browser app for converting Markdown documents to PDF.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "start": "npm run dev",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "smoke": "node scripts/smoke-test.mjs",
    "check": "npm run build && npm run smoke"
  },
  "dependencies": {
    "dompurify": "^3.3.1",
    "jspdf": "^3.0.4",
    "marked": "^17.0.1"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  },
  "engines": {
    "node": ">=18"
  }
}
